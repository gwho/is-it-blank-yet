# Simple Blockchain

This folder contains a minimal blockchain implementation written in modern JavaScript. It includes basic wallet functionality, a proof-of-work mining algorithm and a small HTTP API built with Express.

## Usage

Install dependencies and run the test script:

```bash
cd simple-blockchain
npm install
npm test
```

To start the API server:

```bash
node src/api.js
```
